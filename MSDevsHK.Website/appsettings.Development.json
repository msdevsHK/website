{
  "Host": {
    "Hostname": "local.www.msdevshk.com",
    "HttpsPfxPath": "certs/cert.pfx",
    "HttpsPfxPassword": "password" // Override in secrets store.
  },
  "OAuth": {
    "Meetup": {
      "ClientId": "dev-client-id", // Override in secrets store.
      "ClientSecret": "dev-secret" // Override in secrets store.
    }
  },
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  }
}